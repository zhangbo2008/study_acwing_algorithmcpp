#include <bits/stdc++.h>
using namespace std;
int a, b;                   //定义两个整型变量a和b
int main() {
    scanf("%d%d", &a, &b);  //读入a和b，当然用cin也没毛病
    printf("%d\n", a + b);  //输出a+b，当然用cout也没毛病
    return 0;
} 